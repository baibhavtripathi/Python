'''
All functions in python are first class
    i.e they call be called,pass as argument 
    and can be assigned to other variable.

Higher Order Function ::
    i.e these are functions which accept another function as an argument.

'''

operations = {
    'average': lambda seq: sum(seq)/len(seq)
    
}